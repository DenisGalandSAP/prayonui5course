<mvc:View xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    controllerName="z.project7.controller.View1"
    displayBlock="true">
    <Page id="page2"
        title="{i18n>title}">
        <content>
            <Panel>
                <List items="{path : 'products>/',
                              sorter:{path:'name',descending:true,group:true},
                              filters: [{path:'name',operator:'Contains',value1:'Product'}]
                              }">
                    <items>
                        <ObjectListItem title="{products>name}"
                            type="Navigation"
                            number="{products>price}"
                            press=".onNavigate">
                            <firstStatus>
                                <ObjectStatus text="{=${products>stock} > 0 ? 'In stock' : 'Out of Stock' }"
                                    state="{path: 'products>stock',
						                    formatter: '.onStockCheck'
					                    }" />
                            </firstStatus>
                        </ObjectListItem>
                    </items>
                </List>
            </Panel>
        </content>
    </Page>
</mvc:View>